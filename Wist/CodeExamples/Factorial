import "../../../Libraries/net8.0/StandardLibrary.dll"

main() -> i64 (
    Console:Write(Str:I64ToStr(fact(5)))
    Console:Write(Str:I64ToStr(fact(7)))
    Console:Write(Str:I64ToStr(fact(10)))
    ret 0
)


fact(i64 x) -> i64 (
    if (x <= 2) ( ret x )
    ret x * fact(x - 1)
)