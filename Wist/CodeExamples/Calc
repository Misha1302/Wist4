import "../../../Libraries/net8.0/StandardLibrary.dll"

main() -> i64 (
    i64 i = 0
    
    
while_start:
    
    if (i >= 5) ( goto while_end )
    
    f(i)
    i = i + 1

    goto while_start
while_end:

    ret i
)


f(i64 x) -> none (
    Console:Write(Str:I64ToStr(x))
    ret 0
)