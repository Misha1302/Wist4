import "../../../Libraries/net8.0/StandardLibrary.dll"

main() -> i64 (
    i64 a = 999
    i64 q = 10 * (1 + 2 + 3 + 4 + 5 + 6) * a / (3 * 2)
    Console:Write(Str:I64ToStr(q))
    Console:Write(Str:I64ToStr(f(123)))
    
    // it optimize into 'Console:Write(Str:I64ToStr(34965))'

    ret 0
)

f(i64 a) -> i64 ( ret a )