import "../../../Libraries/net8.0/StandardLibrary.dll"
import "CodeExamples/StdLib"
    

main() -> i64 (
    for (i64 i = 0, i <= 5, i = i + 1,
        printI64NoLn(i)
        printStrNoLn(" ")
        if (i <= 0) ( printStr("Zero") )
        elif (i <= 1) ( printStr("One") )
        elif (i <= 2) ( printStr("Two") )
        elif (i <= 3) ( printStr("Three") )
        else ( printStr("Unknown") )
    )

    ret 0
)
